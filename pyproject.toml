[project]
name = "hephaistos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gil", email = "hi@gildrb.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
]

[project.scripts]
hephaistos = "hephaistos.cli.app:main"
heph = "hephaistos.cli.app:main"

[build-system]
requires = ["uv_build>=0.10.1,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
