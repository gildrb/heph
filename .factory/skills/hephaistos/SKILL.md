---
name: hephaistos
description: Local-first study CLI with armories, source-grounded answers, and a public-facing `heph` command
---

# Hephaistos Project Skill

Use the repo-native docs as the source of truth.

- Read `AGENTS.md` first for commands, conventions, and the telemetry/docs-sync contract.
- Read `docs/architecture.md` for package boundaries and the telemetry design.
- Run `uv run python scripts/sync_docs.py` after changing CLI, telemetry, or README-adjacent docs.
