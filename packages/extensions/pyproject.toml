[project]
name = "heph-extensions"
version = "0.0.51"
description = "Stable extension contracts for Heph and Hephaion"
readme = "README.md"
license = "MIT"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build==0.11.11"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = ["extensions"]
module-root = "src"
