# Heph Extension Contracts

Extensions owns stable contracts for user-extensible prompts, tools, and
workflows. Concrete app, TUI, and provider implementations should depend on
these contracts instead of modifying harness loops directly.
