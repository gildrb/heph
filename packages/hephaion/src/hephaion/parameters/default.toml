# Default parameters for Heph.
# Model access, model selection, and global app preferences are configured via
# /login, /logout, /models, /settings, or environment variables - not here.

max_tokens = 4096

# Logging configuration (overridden by HEPHAION_LOG_* env vars)
# log_level = "WARNING"     # DEBUG, INFO, WARNING, ERROR
# log_file = ""             # Path to append-only log file
# log_format = "json"      # "json" or "text"

# Local diagnostics
# Trace files live under <armory>/.hephaion/traces/
# CPU profiles are written to ~/.cache/hephaion/profiles/
