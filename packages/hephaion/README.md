# Hephaion

Harness package root.

`hephaion/` owns loops, validation, event streaming, retrieval, citations, session
persistence, runtime calls, and policy guardrails. It should compose Heph behavior
through narrow prompt/state contracts instead of hardcoding phrase-specific answers.
